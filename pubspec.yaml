name: flutter_authantication_sample
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  firebase_auth:
  cloud_firestore:
  fluttertoast:
  google_sign_in:
  flutter_facebook_login:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true